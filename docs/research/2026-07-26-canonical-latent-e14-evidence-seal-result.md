# Canonical Latent E14 Evidence-Seal Result

Date: 2026-07-26
Status: `e13_scientific_result_sealed`

## Decision

E14 independently seals the immutable E13 scientific result as
`full_parameterization_deterministic_recovery_succeeds`.

The E12 failure is therefore not attributable, within the frozen smooth
periodic linear advection-diffusion family, to E10 representation
insufficiency, missing excitation rank, or inability of the full-skew
generator class to express the identified law. It belongs to the frozen E12
stochastic AdamW/scheduled-batch training package.

This is a package-level assignment, not a claim that L-BFGS alone is causal.
Do not hard-code Fourier tying, reopen the encoder, introduce routing, or
broaden the physics claim. The full-skew controls retain a nearly extinguished
eight-step relative-error caveat.

## Why E14 was required

E13 completed two byte-identical scientific runs, but its original runner
mislabeled canonical payload hashes as raw-file hashes and emitted neither a
compact result nor a detached manifest. Independent E13 result review therefore
issued evidence NO-GO even though the scientific recomputation passed.

The frozen E13 contract prohibited post-state source repair. E14 consequently
performed a separate zero-state evidence seal over the already captured bytes.
It did not rerun a state, dataset, generator, model, optimizer, oracle, or
metric.

The E14 contract was independently reviewed through three correction rounds:

1. replace direct output writes with all-or-nothing sibling-directory
   publication and independently recompute leaf evidence;
2. correct counterfactual classification wiring and cross-bind every reopened
   declaration; and
3. place publication and lock finalization under one cleanup boundary.

The final focused suite passed `43/43` before sealing.

## Frozen provenance and inputs

E14 sealed from clean HEAD
`63e23d50f2eef3ca2644100674c10c4e2aa3a5ba`.
The original E13 execution HEAD remains
`fe47b937205f47a2dba93f0ecbeee83015824c09`.

Literal E13 input hashes and byte counts:

| Input | Bytes | Raw SHA-256 |
| --- | ---: | --- |
| Replicate A | `6,648,184` | `e0bc7b0575a8c6982ecc9fbff3899be0792643a57575a2349468a0244e7e6d9c` |
| Replicate B | `6,648,184` | `e0bc7b0575a8c6982ecc9fbff3899be0792643a57575a2349468a0244e7e6d9c` |
| Combined | `6,648,535` | `f65cdc9b6b965b5e77715ae5b00d1ecc21dd683b7ffe09c8ce19235d3283159a` |

Canonical payload SHA-256 values are:

- replicate:
  `d04d71e7b528d2332ca9de98bb64d6f065dff4614d82a2416ec5330ce3a41f19`;
- combined:
  `022f8867fab4f9b28f84da501245928ee3ef3f074dc6d1a84ed04c56bad7abbb`.

The raw replicate bytes are identical, the parsed replicate objects are equal,
and the combined object with its `replication` field removed equals the
replicate object.

## Independent scientific recomputation

E14 independently recovered from stored leaves:

- the exact E12 checkpoint, three generator hashes, two losses, update/example
  counts, basis action, and rollout;
- all seven dataset locks and five schedule locks;
- full-skew, support-sparse, and mode-tied parameter counts, masks, structures,
  and oracle phase;
- all 18 oracle and E11 projection-closure cases and maxima;
- all numerical-rank tolerances and condition numbers from stored singular
  values;
- exact coverage over six controls, 24 validation cells, 10,584 unique
  mode-resolved records, and 30 literal argmax records; and
- every recovery gate and the registered classification precedence.

Excitation is complete:

- all three covariances rank `48/48`;
- all 42 rotation-plane Grams rank `2/2`;
- the mode-tied Jacobian ranks `12/12` with condition number
  `14.27261570008385`.

Key outcomes:

| Control | Basis-action NRMSE | Composite rollout NRMSE | Final HF NRMSE | Recovery |
| --- | ---: | ---: | ---: | --- |
| E12 AdamW replay | `0.0999215` | `0.00536402` | `0.261281` | fail |
| Full-skew L-BFGS neutral | `8.17e-6` | `6.54e-6` | `1.72e-5` | pass |
| Full-skew L-BFGS polish | `1.06e-5` | `5.82e-7` | `2.31e-5` | pass |
| Support-sparse L-BFGS | `4.06e-7` | `8.38e-7` | `4.41e-8` | pass |
| Mode-tied L-BFGS | `1.03e-7` | `1.39e-7` | `1.10e-7` | pass |
| Oracle | `0` | `1.42e-14` | `8.33e-13` | pass |

Because both full-skew deterministic controls pass before support restriction
or mode tying is considered, the frozen precedence yields
`full_parameterization_deterministic_recovery_succeeds`.

## Preserved caveat

The registered but ungated eight-step per-basis relative maximum remains:

- neutral: basis `41`, `composite_c`, horizon `8`, NRMSE
  `0.3595701757249822`;
- polish: basis `41`, `composite_c`, horizon `8`, NRMSE
  `0.37147804128286543`.

This is relative error on a target attenuated to near zero under maximum
diffusion. It is neither averaged away nor assigned a post-hoc threshold.

## Sealed outputs

The directory
`docs/research/artifacts/canonical_latent_e14_evidence_seal` contains exactly:

| Output | Bytes | Raw SHA-256 |
| --- | ---: | --- |
| Evidence bundle | `1,350,509` | `a4886e3b3a8c678abe7b8f44907b8655af4b0ac68fb47ca9353ae2bfca677b5c` |
| Compact result | `29,796` | `a26b4948a0db7fd7aa1a0c067bf29eaacd3007e4c93e1bb309a5c610cad04413` |
| Detached manifest | `8,111` | `88f45edfa0c456ec19eef8da8167ed0f2bf6213f13f00ef6f32d934c4cf9257e` |

Independent post-seal review verified the directory set, every raw and
canonical hash, Git objects, byte counts, archive member bytes and metadata,
compact/manifest cross-binding, scientific recomputation, and classification.

E14 records zero state reads, optimizer updates, held-out reads, provider
calls, and routing paths. It explicitly leaves E13's original evidence seal
nonconforming and unmodified.

## Next gate

Preregister E15 as a practical training-package challenger:

1. keep E10, the full structured generator, E12 data, and all qualification
   gates frozen;
2. separate optimizer, batching, and blockwise/curriculum effects with a
   minimal factorial control set;
3. include deterministic full-batch recovery as a ceiling, not a deployment
   recipe by fiat;
4. preserve the nearly extinguished-mode absolute-amplitude context without
   changing the E13 gate;
5. require multiple frozen initializations or an equivalently preregistered
   robustness test before claiming a practical repair; and
6. keep routing, Fourier hard-coding, encoder changes, extra physics, and
   post-hoc thresholds closed.
